platform :macos, '13.0'

target 'ScreenRecorder' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ScreenRecorder
  # We'll be using native frameworks for now, but we can add dependencies as needed
end 